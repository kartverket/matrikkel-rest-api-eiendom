name: Promote Test to Prod
on: workflow_dispatch

jobs:
  deploy-argo:
    permissions:
      id-token: write
    uses: kartverket/smia-apps/.github/workflows/promote-app-octo-sts.yml@main
    with:
      application: eiendom
      namespace: smia-tjenester-main
      extra_namespace: smiatest-tjenester-main
      identity: 'matrikkel-rest-api-eiendom'
